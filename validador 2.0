// Function to clear all car data
function clearAllCarData() {
    carFileNames.length = 0;
    window.analysisFeatures = window.analysisFeatures || []; window.analysisFeatures.length = 0; window.environmentalMetrics = null; const envContent = document.getElementById('environmental-content'); if (envContent) envContent.innerHTML = '';
    // Existing code...
}